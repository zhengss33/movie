extends ../layout.pug

block content
  .container
    .row
      each item in movies
        .col-md-2
          .thumbnail
            a(href=`/movie/${item._id}`)
              img(src=item.poster, alt=item.title, width="100%")
            .caption
              h6 #{item.title}
              p: a.btn.btn-primary(herf=`/movie/${item._id}` role="button")
                | 观看影片
